/**
 * Application Status Constants
 */
export const STATUS = {
  PENDING: 'Pending',
  REVIEWED: 'Reviewed',
  SHORTLISTED: 'Shortlisted',
  REJECTED: 'Rejected',
};

export const STATUS_ENUM = Object.values(STATUS);

export default {
  STATUS,
  STATUS_ENUM,
};
